# ==============================================================================
# WordPress & MariaDB Docker Compose Environment Configuration (Template)
# ==============================================================================

# Database Settings
DB_NAME=wordpress
DB_USER=wordpress_user
DB_PASSWORD=change_this_to_a_secure_password
DB_ROOT_PASSWORD=change_this_to_a_secure_root_password

# WordPress Settings
WP_PORT=8080
